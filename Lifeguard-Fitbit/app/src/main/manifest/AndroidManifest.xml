<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.example.lifeguard"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <!-- No camera, no activity recognition as requested -->
    <!-- Sensors (accelerometer) don't need runtime permissions -->

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">

        <!-- Handles Fitbit OAuth redirect -->
        <activity android:name=".FitbitAuthActivity">
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <!-- Updated redirect URI scheme for LifeGuard -->
                <data android:scheme="lifeguard" android:host="callback"/>
            </intent-filter>
        </activity>

        <activity android:name=".DashboardActivity"/>
        <activity android:name=".SensorsActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>
</manifest>
